/*
 theBitRiddler
 3/30/2023
 11:18 AM
 Write for statements
*/
#include <stdio.h>
int main(void) {
	// (a)
	for (int x = 1; x <= 7; ++x)
		printf("%d ", x);
		puts("\n");
		
	// (b)
	for (int x = 3; x <= 23; x += 5)
		printf("%d ", x);
		puts("\n");
		
	// (c)
	for (int x = 20; x >= -10; x -= 6)
		printf("%d ", x);
		puts("\n");
		
	// (d)
	for (int x = 19; x <= 51; x += 8)
		printf("%d ", x);
		puts("\n");
}
