NewToken:


DO WHILE
Best Do while use in
instructor's manual CHowToProgram 4.36

int x = 0;
do {
  printf( "%s", "Enter between 5 and 10" );
  scanf( "%d", &x );
while ( x < 5 || x > 10 );

This will go no where until the condition is met.
equal to:

int x = 0;
printf( "%s", "Enter between 5 and 10" );
scanf( "%d", &x );
while ( x < 5 || x > 10 ) {
  printf( "%s", "Enter between 5 and 10" );
  scanf( "%d", &x );
}
